import { getAndRemoveAllAfterLast } from '@writetome51/array-get-and-remove-all-after';


// value cannot be an object.

export function removeAllAfterLast(value: any, array): void {
	getAndRemoveAllAfterLast(value, array);
}
