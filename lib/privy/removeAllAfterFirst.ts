import { getAndRemoveAllAfterFirst } from '@writetome51/array-get-and-remove-all-after';


// value cannot be an object.

export function removeAllAfterFirst(value: any, array): void {
	getAndRemoveAllAfterFirst(value, array);
}
